{
  "name": "pwpaddon",
  "version": "1.0.5",
  "license": "MIT",
  "author": "Bastian Leicht",
  "scripts": {
    "build": "web-ext build --ignore-files dist/ webpack.*",
    "test": "web-ext run --verbose --browser-console --start-url example.com"
  },
  "dependencies": {
    "web-ext": "^7.12.0"
  }
}
